# Skills

在本目录新增一个 UTF-8 Markdown 文件即可注册 skill，文件名（不含 `.md`）就是 skill 名。

例如 `重装系统.md` 可通过 `@柯基服务队 /重装系统 我的电脑无法启动` 显式指定。即使没有斜杠命令，模型也会根据问题内容主动采用相关 skill。

当前 skill 仅包含知识和回复规范，不应声称执行了外部操作。`README.md` 本身不会被加载。
